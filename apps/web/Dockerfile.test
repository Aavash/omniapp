# Test Dockerfile for WorkPulse Web
FROM node:18-alpine

# Set working directory
WORKDIR /app

# Copy package files
COPY package*.json ./

# Install dependencies
RUN npm ci

# Copy source code
COPY . .

# Default command runs tests
CMD ["sh", "-c", "npm run lint && npx tsc --noEmit && npm run build"]sdf